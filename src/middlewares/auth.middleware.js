const { verify } = require('../ultis/jwt.util')
const { JsonWebTokenError, TokenExpiredError } = require('jsonwebtoken')

const authMiddleware = async (req, res, next) => {
	try {
	} catch (error) {}
}

module.exports = authMiddleware
